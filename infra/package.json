{
  "name": "checkout-infra",
  "version": "1.0.0",
  "private": true,
  "description": "Pulumi infrastructure for the e-shop checkout system",
  "scripts": {
    "build:lambdas": "node ./scripts/build-lambdas.mjs",
    "typecheck": "tsc --noEmit",
    "preview": "pulumi preview",
    "up": "pulumi up"
  },
  "dependencies": {
    "@pulumi/aws": "^7.10.0",
    "@pulumi/mongodbatlas": "^4.4.0",
    "@pulumi/pulumi": "^3.185.0",
    "@pulumi/random": "^4.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "esbuild": "^0.25.9",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
